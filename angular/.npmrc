# This project has a few ABP packages with peer-dependency ranges that don't
# perfectly line up (see package.json history). legacy-peer-deps keeps `npm
# install`/`npm ci` from failing on those instead of everyone needing to
# remember to pass the flag by hand.
legacy-peer-deps=true
